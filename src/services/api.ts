/**
 * @deprecated
 * Este arquivo existia como camada de orquestração entre apiClient e os hooks.
 * Foi substituído pelo padrão requests + react-queries (igual ao next-marvel).
 *
 * Imports migrados para:
 *   patients  → src/features/patients/
 *   prediction → src/features/prediction/
 */
export {};
