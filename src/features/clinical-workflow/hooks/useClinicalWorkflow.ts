// Re-export from original location during migration window.
// TODO: inline the full hook body here once all imports are updated.
export { useClinicalWorkflow } from '../../../features/clinical-workflow/hooks/useClinicalWorkflowImpl';
